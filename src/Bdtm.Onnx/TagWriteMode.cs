namespace Bdtm.Onnx;

public enum TagWriteMode
{
    /// <summary>Replace all existing tags.</summary>
    ReplaceAll,
    /// <summary>Append tags that are not already present.</summary>
    AppendNew,
}
